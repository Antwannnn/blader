export enum LengthParameter {
    SHORT = 10,
    MEDIUM = 30,
    LONG = 50,
    VERY_LONG = 70,
}

export enum SentenceParameter {
    QUOTE,
    RANDOM,
    // NOT IMPLEMENTING IN THE FIRST VERSION
    CUSTOM,
    MIRROR,
}

export enum GameTypeParameter {
    TYPE_TESTER,
    TYPE_RACER,
}

export enum GameState{
    STARTED,
    RESET,
    ENDED
}

